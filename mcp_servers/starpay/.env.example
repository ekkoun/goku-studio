# StarPay 数据库连接配置（只读报表账号）
# 复制此文件为 .env 并填入实际值

STARPAY_DB_HOST=127.0.0.1
STARPAY_DB_PORT=3306
STARPAY_DB_USER=starpay_report_ro
STARPAY_DB_PASS=
STARPAY_DB_NAME=starpay_prod

# 时区配置（DB 存储时区，转 JST 时使用）
# UTC  → JST: +9h
# Asia/Tokyo → 无需转换
STARPAY_DB_TZ=UTC
